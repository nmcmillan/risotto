<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.hello" android:versionCode="1" android:versionName="1.0">
	
	<!-- Application Permissions -->
    <uses-permission android:name="android.permission.VIBRATE"></uses-permission>
    <uses-permission android:name="android.permission.FLASHLIGHT"></uses-permission>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"></uses-permission>

	<!-- Application Declaration -->
	<application android:icon="@drawable/icon" android:label="@string/app_name" > android:launchMode="singleInstance">

		<!-- Launch Activity -->
		<activity android:name=".HelloWorld" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar">
		
			<intent-filter>
				<!-- Main entry activity in this application node. -->
				<action android:name="android.intent.action.MAIN" />
				<!-- Specifies that this Activity can be launched from the main launcher. -->
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
			
		</activity>
		
		<!-- ContentProvider Node -->
		<provider android:name="com.risotto.storage.StorageProvider"
            android:authorities="com.risotto.storage.provider" />
		
		<!-- Drug View Activity -->
		<activity android:name="com.risotto.view.DrugView"/>
		
		<activity android:name="com.risotto.view.AlarmView"/>
		
		<!-- Dialog Activity -->
		<activity android:name="DialogActivity" android:theme="@android:style/Theme.Dialog" android:excludeFromRecents="true"/>

		<!-- Receiver Node -->
        <receiver android:name="com.risotto.service.MainServiceManager">
        	<intent-filter>
        		<action android:name ="android.intent.action.BOOT_COMPLETED"/>
        		<action android:name ="android.intent.action.USER_PRESENT"/>
        	</intent-filter>
        </receiver>
        
        <!-- Drug Add/Edit activity -->
        <activity android:name="com.risotto.view.DrugEdit"
        	android:windowSoftInputMode="stateVisible"
        	android:label="@string/drug_edit_title"
        	android:theme="@android:style/Theme.Dialog">
        		<intent-filter android:label="@string/drug_resolve_title">
        			<action android:name="com.risotto.action.EDIT_DRUG"/>
        			<category android:name="android.intent.category.DEFAULT" />
	                <!-- ALTERNATIVE: show as an alternative action when the user is
	                     working with this type of data. -->
	                <category android:name="android.intent.category.ALTERNATIVE" />
	                <!-- SELECTED_ALTERNATIVE: show as an alternative action the user
	                     can perform when selecting this type of data. -->
	                <category android:name="android.intent.category.SELECTED_ALTERNATIVE" />
	                <!-- This is the data type we operate on. -->
	                <data android:mimeType="vnd.android.cursor.item/vnd.google.note" />
        		</intent-filter>
        	
        </activity>
		
		<!-- Main Service -->
		<service android:name="com.risotto.service.MainService"/>
		
	</application>
	
	<!-- Minimum SDK Version -->
	<uses-sdk android:minSdkVersion="7" />

</manifest> 